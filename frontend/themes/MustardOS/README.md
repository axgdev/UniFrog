Fallback MuOS glyph assets for the native frontend adapter.

MuOS themes can omit glyph images and rely on built-in defaults from
MustardOS.muxthm. UniFrog packages this small fallback set so downloaded
.muxthm themes still get visible launcher icons on SF2000.
