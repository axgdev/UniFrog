# Third-Party Notices

UniFrog source files are MIT licensed unless a file or directory states
otherwise. This repository also builds, links, or packages third-party
components under their own licenses. Keep this file updated when adding,
removing, or replacing a third-party input.

This is a license inventory and attribution aid, not legal advice.

## Source Layout Policy

- `.deps/` is generated by `make deps` and contains pinned upstream checkouts.
  Do not commit `.deps`; update pins in `config/options.mk` or
  `cores/manifest.mk` and keep UniFrog dependency edits as generated patch
  queues under `patches/` or `cores/patches/`.
- `unifrog-hcrtos-sdk/` is a submodule. It is a mixed-license SDK staging tree,
  not MIT-only code. See `unifrog-hcrtos-sdk/LICENSE` and
  `unifrog-hcrtos-sdk/THIRD_PARTY_NOTICES.md`.
- `build/`, `output/`, `cores/output/`, `frontend/output/`, and the standalone
  JS2300 output are generated artifacts and are not source of truth for
  license review.

## Fetched Build Inputs

These inputs are fetched by `make deps` and pinned by the root `Makefile` or
`cores/manifest.mk`.

| Component | Fetched Location | Used For | License / Notice |
| --- | --- | --- | --- |
| frog2k-javascript | `.deps/frog2k-javascript` | Standalone JS2300 runtime, bindings, and vendored MQuickJS | MIT for JS2300; see the checkout README and `vendor/mquickjs/LICENSE`. |
| libretro-common | `.deps/cores/libretro-common` | Libretro support objects | File-level notices, mostly MIT-style/libretro permissive code plus public-domain `libco`. Only the paths listed in `cores/Makefile` are compiled. |
| libchdr | `.deps/support/libchdr` | CHD support for core modules | BSD 3-Clause style notice. See `.deps/support/libchdr/LICENSE.txt`. |
| LZ4 | `.deps/support/lz4` | LZ4-compressed libretro content support | BSD 2-Clause. See `.deps/support/lz4/lib/LICENSE`. |
| xxHash | `.deps/support/lz4/lib/xxhash.*` | LZ4 frame support | BSD 2-Clause notice in source headers. |
| NanoSVG | `.deps/support/nanosvg` | Native reader SVG parse and raster fallback | zlib-style permissive license. See `.deps/support/nanosvg/LICENSE.txt`. |
| stb_image | `.deps/support/stb/stb_image.h` | Native reader raster image decode fallback | Public domain dedication with MIT alternative. See file header and footer. |
| stb_truetype | `.deps/support/stb/stb_truetype.h` | Font rasterization for native UI text | Public domain dedication with MIT alternative. See file header and footer. |
| zlib | `.deps/support/zlib` | Boot-logo PNG decode helper, CHD support, and core support | zlib license. See `.deps/support/zlib/LICENSE`. |
| Zstandard | `.deps/support/zstd` | Zstd decode support | BSD 3-Clause. See `.deps/support/zstd/LICENSE`. |
| HCRTOS SDK | `unifrog-hcrtos-sdk/` | Headers, libraries, kernel rebuild, media module | Mixed open-source and vendor terms. See the SDK license and third-party notice files. |

## Libretro Core Modules

The SD-card package includes binary libretro core modules built from pinned
upstream checkouts plus generated UniFrog patch queues under `cores/patches/`.

| Module | Source Checkout | License / Notice |
| --- | --- | --- |
| Gambatte | `.deps/cores/gambatte-libretro` | GPL-2.0. See `COPYING`. |
| gpSP | `.deps/cores/gpsp-libretro` | GPL-2.0. See `COPYING`. |
| gpSP GBAC+Prosty | `.deps/cores/gpsp_multicore` | GPL-2.0. See `COPYING`. |
| PicoDrive | `.deps/cores/picodrive` | Non-commercial source license in `COPYING`; bundled subcomponents have their own notices. |
| Snes9x 2005 | `.deps/cores/snes9x2005` | Mixed Snes9x non-commercial and libretro-common permissive notices. See `copyright` and file headers. |
| Snes9x 2002 | `.deps/cores/snes9x2002` | Snes9x non-commercial notice in source headers. |
| QuickNES | `.deps/cores/QuickNES_Core` | GPL-2.0. See `LICENSE`. |
| FCEUmm | `.deps/cores/libretro-fceumm` | GPL-2.0. See `Copying`. |
| Gearboy | `.deps/cores/Gearboy` | GPL-3.0. See `LICENSE`. |
| Beetle PCE Fast | `.deps/cores/beetle-pce-fast-libretro` | GPL-2.0. See `COPYING`. |
| QPSX | `.deps/cores/sf2000-qpsx-playstation-emulator` | GPL-2.0-or-later. See `LICENSE`. |
| FrogUI | `.deps/cores/FrogUI` | CC BY-NC-SA 4.0. The license prohibits commercial use, including bundling with devices for sale. See `LICENSE`. UniFrog adaptations are maintained as a patch queue. |
| MAME 2000 | `.deps/cores/libretro-mame2000` | Historical MAME non-commercial license in `readme.txt`; bundled sources also carry file-level notices. |
| FB Alpha 2012 | `.deps/cores/fbalpha2012` | Historical FB Alpha/MAME non-commercial terms and component notices. See `svn-current/trunk/src/license.txt`. |
| Stella 2014 | `.deps/cores/libretro-stella2014` | GPL-2.0. See `stella/license.txt`. |
| A5200 | `.deps/cores/a5200` | GPL-2.0. See `License.txt`. |
| ProSystem | `.deps/cores/libretro-prosystem` | GPL-2.0. See `License.txt`; bundled components retain their own notices. |
| Atari800 library | `.deps/cores/libretro-atari800lib` | GPL-2.0. See `atari800/COPYING`. |
| Handy | `.deps/cores/libretro-handy` | zlib-style permissive license. See `lynx/license.txt`. |
| RACE | `.deps/cores/RACE` | GPL-2.0. See `license.txt`. |
| Beetle Cygne | `.deps/cores/libretro-beetle-wswan` | GPL-2.0. See `COPYING`. |
| Gearcoleco | `.deps/cores/Gearcoleco` | GPL-3.0. See `LICENSE`. |
| Snes9x 2005 Prosty | `.deps/cores/snes9x2005-prosty` | Mixed MIT, GPL-2.0-or-later, and component notices. See `copyright` and source headers. |
| Snes9x 2002 Prosty | `.deps/cores/snes9x2002-prosty` | Snes9x non-commercial license. See `src/copyright.h`. |
| Gambatte Prosty | `.deps/cores/libretro-gambatte-prosty` | GPL-2.0. See `COPYING`. |
| QuickNES Prosty | `.deps/cores/QuickNES_Core-prosty` | GPL-2.0. See `LICENSE`. |
| Frodo Prosty | `.deps/cores/libretro-frodo-prosty` | GPL-2.0. See `COPYING`. |
| Fake-08 Prosty | `.deps/cores/fake-08-prosty` | MIT with bundled MIT, WTFPL, zlib, and other component terms. See `LICENSE.MD` and bundled library notices. |
| blueMSX Prosty | `.deps/cores/libretro-blueMSX-prosty` | Mixed BSD-style, GPL, freeware, and component terms. See `license.txt` and source headers. |
| FCEUmm Prosty | `.deps/cores/libretro-fceumm-prosty` | GPL-2.0. See `Copying`; NTSC filter code has its own notice. |

## Assets

The SD artifact includes `unifrog-ui.ttf`, a renamed, reduced-glyph derivative
of Noto Sans JP for the languages supported by the frontend and common Japanese
filenames. It is distributed under the SIL Open Font License 1.1; the complete
license is installed beside the font as `/unifrog_data/fonts/OFL.txt`.

The Polish Latin Extended-A glyphs (Aogonek through zdotaccent) that Noto Sans
JP lacks were transplanted as decomposed outlines from DejaVu Sans
(Bitstream Vera derivative, free license with an embedded-font exemption; see
`https://dejavu-fonts.github.io`). Their inclusion keeps the whole font
redistributable under the SIL Open Font License 1.1 alongside the existing
notice.

FrogUI packages GamePocket (SIL Open Font License 1.1) and Monogram (CC0) in
`/unifrog_data/frogui/fonts`; their complete notices are installed beside them.

The boot logo at `assets/boot/unifrog-logo.png` and the default system icon
theme under `components/frontend/assets/themes/system-icons/` are original
UniFrog assets and use
the UniFrog MIT license unless replaced by a downstream distribution.

## Binary Distribution Notes

Release SD-card archives include firmware, runtime modules, and libretro core
modules. The package installs this notice as `/unifrog/THIRD_PARTY.md`, the
project license as `/unifrog/LICENSE.txt`, and verbatim license material for
each packaged core under `/unifrog/licenses/`.

For GPL and other copyleft components, the corresponding source is the exact
UniFrog repository revision used for the build, plus the pinned upstream
checkouts reproduced by `make deps`. For SDK-linked binaries, also preserve the
SDK license and notice files and the build scripts needed to relink.
