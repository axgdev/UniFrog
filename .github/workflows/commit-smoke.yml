name: Commit smoke

on:
  push:
    branches: [main]
  pull_request:

jobs:
  commit-smoke:
    runs-on: ubuntu-latest
    container:
      image: alpine:latest
    steps:
      - name: Install packages
        run: apk add git make dtc tcc tcc-libs-static musl-dev zlib-dev curl tar xz zip patch

      - name: Check out UniFrog
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: false

      - name: Configure Git
        run: |
          set -eu
          export GIT_CONFIG_GLOBAL="$RUNNER_TEMP/unifrog-gitconfig"
          git config --global --add safe.directory "$PWD"
          git config --global user.name "UniFrog CI"
          git config --global user.email "ci@unifrog.local"
          git config --global \
            url."https://github.com/axgdev/frog2k-qpsx.git".insteadOf \
            "git@github.com:axgdev/frog2k-qpsx.git"
          git config --global \
            url."https://github.com/axgdev/frog2k-javascript.git".insteadOf \
            "git@github.com:axgdev/frog2k-javascript.git"
          echo "GIT_CONFIG_GLOBAL=$GIT_CONFIG_GLOBAL" >> "$GITHUB_ENV"
          echo "GIT_TERMINAL_PROMPT=0" >> "$GITHUB_ENV"

      - name: Cache core sources
        uses: actions/cache@v4
        with:
          path: |
            .deps/cores
            .deps/support
          # Smoke runs only fetch the fast core set, so keep their snapshots on
          # their own key: a partial .deps/cores must never be restored by the
          # release sd-zip job (which needs every core checkout).
          key: unifrog-cores-smoke-${{ runner.os }}-${{ hashFiles('cores/manifest.mk', 'cores/patches/**/*.patch') }}
          restore-keys: |
            unifrog-cores-smoke-${{ runner.os }}-
            unifrog-cores-${{ runner.os }}-

      - name: Cache toolchain
        uses: actions/cache@v4
        with:
          path: toolchain
          key: unifrog-toolchain-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Makefile') }}
          restore-keys: |
            unifrog-toolchain-${{ runner.os }}-${{ runner.arch }}-

      - name: Check current commit
        run: |
          make ci-commit-smoke HOSTCC=tcc TOOLCHAIN="$PWD/toolchain"
