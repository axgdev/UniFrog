# System Icons Theme

These PNG icons were generated for UniFrog and may be used under the same
license terms as the frontend repository. They are original simple pixel-art
marks, not copied console logos or third-party icon assets.

Theme authors can replace files in `icons/` and point `theme.ini` keys at the
new PNGs.
